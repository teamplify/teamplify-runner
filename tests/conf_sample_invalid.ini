[unknown_section]
with = lots
of = keys
in = it

[web]
port = gav
use_ssl = not sure

[db]
host = -1 # comment after value
port = 70000
unknown_key = lol
backup_mount = not a valid path

[email]
address_from = not even a email
smtp_protocol = tcp

[main]
product_key = 42
